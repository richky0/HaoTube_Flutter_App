name: haotube_working
description: "HaoTube - Aplikasi Video Streaming"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Dependensi UI dan Utilitas ---
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0

  # --- Dependensi Jaringan dan Data ---
  http: ^1.5.0
  url_launcher: ^6.3.2

  # --- Dependensi Video Streaming ---
  youtube_player_flutter: ^9.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true